# Anthropic API Key for Claude AI analysis
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_api_key_here

# Google Places API Key for reviews analysis
# This tool uses BOTH Places APIs for best results:
# - Places API (New): For search and basic data
# - Places API (Legacy): For detailed reviews (up to 20)
#
# Setup (enable BOTH APIs):
# 1. Go to: https://console.cloud.google.com/
# 2. Create a new project or select existing one
# 3. Enable "Places API (New)": https://console.cloud.google.com/apis/library/places.googleapis.com
# 4. Enable "Places API" (legacy): https://console.cloud.google.com/apis/library/places-backend.googleapis.com
# 5. Create credentials (API Key) in "Credentials" section
# 6. Wait 2-3 minutes for API activation to propagate
#
# The SAME API key works for both APIs!
GOOGLE_PLACES_API_KEY=your_google_api_key_here

# HTTP Server Port (for MCP HTTP server)
# Railway automatically sets PORT - don't override in production
# For local development, defaults to 3000 if not set
PORT=3000
